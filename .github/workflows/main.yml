name: Email Step Check

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f2002971@ds.study.iitm.ac.in
        run: echo "This step contains my email"

      - name: Done
        run: echo "Workflow finished"
